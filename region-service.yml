#Application port
server:
  port: 8082

#Eureka server uri
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8090/eureka

#database details
db:
  connection:
    host: 0.0.0.0
    port: 27018
    database: regionDB
