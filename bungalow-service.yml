#Server-Port
server:
  port: 8081

  #Eureka server uri
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8090/eureka

#database details
db:
  connection:
    host: localhost
    port: 27017
    database: bungalowDB